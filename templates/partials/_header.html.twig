<nav class="bg-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <a href="{{ path('homepage') }}" class="text-2xl font-bold text-indigo-600">HandleBook</a>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                    <a href="{{ path('homepage') }}" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        Livres
                    </a>
                    {% if is_granted('ROLE_USER') %}
                        <a href="{{ path('app_book_new') }}" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            Ajouter un livre
                        </a>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('admin') }}" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            Administration
                        </a>
                    {% endif %}
                </div>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:items-center">
                {% if is_granted('ROLE_USER') %}
                    <div class="ml-3 relative">
                        <span class="text-gray-700">{{ app.user.username }}</span>
                        <a href="{{ path('app_logout') }}" class="ml-4 text-gray-500 hover:text-gray-700">Déconnexion</a>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-gray-500 hover:text-gray-700">Connexion</a>
                    <a href="{{ path('app_register') }}" class="ml-4 text-gray-500 hover:text-gray-700">Inscription</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
